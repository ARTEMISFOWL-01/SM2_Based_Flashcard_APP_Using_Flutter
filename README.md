# SM2_Based_Flashcard_APP_Using_Flutter
Uses simplifies SM-2(Super-Memo-2 without ease_factor decreasing function(remain constant instead of decreasing))  using flutter dart.
##setup
take the lib folder into flutter project and run <flutter pub get > in terminal then run the app
